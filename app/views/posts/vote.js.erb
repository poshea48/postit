<% if @vote.valid? %>
  $("#post_<%= @post.id %>_votes").html("<%= @post.total_votes %> votes")
<% else %>
  alert("You can only vote once on a post")
  // let a = $("a").addClass("close").attr("data-dismiss", "alert").text("&#215;")
  // let div = $("div").addClass("alert alert-error").append()
  // $("#ajax_message")
<% end %>
